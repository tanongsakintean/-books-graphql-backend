type Book {
    bookId: ID
    title: String
    isbn: String
    publicDate: String
    publisherId: Int
    genreId: Int
    summary: String
    pageCount: Int
}

type BookPublisher {
    book: Book
    publisher: Publisher
    genre: Genre
}

type BookAuthor {
    author:Author
    books:[BookPublisher]
}


input BookInput {
    title: String
    isbn: String
    publicDate: String
    publisherId: Int
    genreId: Int
    summary: String
    pageCount: Int
    authorId: [Int]
}
