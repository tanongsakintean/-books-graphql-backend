type Review {
    reviewId: ID
    bookId: Int
    userId: Int
    rating: Int
    comment: String
    createdAt: String
}

type ReviewDetail {
    review: Review
    user: User
    book: Book
}

input ReviewInput {
    bookId: Int
    userId: Int
    rating: Int
    comment: String
}